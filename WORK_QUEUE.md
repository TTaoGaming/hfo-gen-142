# Gen142 Recovery Work Queue

Parent coordination: issue #1. Workers self-select; Tao does not route.

## Wave A — identity + memory
Recover mission, ontology, checkpoint, capsule, continuity, and anti-context-loss patterns. Gatherer proposes compact genes; Falsifier attacks staleness/redundancy/operator burden.

## Wave B — MAPE-K + Byzantine control
Recover monitor/analyze/plan/execute/knowledge loops, conflict handling, independent readback, fail-closed rules, and scars from contradictory agents.

## Wave C — Cloudflare + VPS substrate
Recover native Cloudflare ownership boundaries and Oracle/OVH execution patterns. Fresh evidence outranks historical success. Delete duplicate custom control-plane ideas.

## Wave D — evolution + external fitness
Recover frozen-evaluator separation, COTS evolution/QD mechanisms, reducer logic, competition/world-record/income proof surfaces, and measured external-fitness criteria.

## Wave E — capabilities + WorldWeaver
Recover MCP/Agent Skills/capability admission patterns and the public/LLM projection intent behind WorldWeaver. Do not assume the prior website implementation is correct.

## Wave F — resource scars + heritage red team
Recover disk-fill, cache/worktree, retry-storm, branch-sprawl, and operator-overload scars. Red-team proposed genes and kill anything that adds a failure boundary without deleting another.

## Worker output
Each worker uses a fresh UUID, reads issue #1 newest-first, checks existing coverage, performs one bounded evidence-first cell, and writes one compact terminal comment to #1. Classify donor findings ADOPT / ADAPT / REFERENCE / HOLD / KILL.

The queue is a work menu, not proof that independent workers are currently running.