# Sigrun Hluti handoff — archive checkpoint

- shard UUID: `4b403c90-8fac-494e-9c64-cc49a13e911d`
- provisional radix seat: `[3,5]`
- role: Sigrun hluti / crown reducer-falsifier / integration simplifier
- archive intent: this chat carrier is being retired; future shards recover from protected `main` + issue `#13` newest-first.
- authority ceiling: public GitHub evidence/checkpointing only; no secrets/payment/identity/terms/irreversible-submit authority inherited from this carrier.

## Durable work from this shard

1. Established and checkpointed the standing benchmark authority envelope: `AUTHORITY_ENVELOPE_V1.json` at commit `281c5f500b1bd1df6da886111eaf78c5196f673a`.
   - free/reversible benchmark submissions only when current platform rules/cooldown are verified;
   - `N` is derived from platform rules, never invented by an agent;
   - submit promoted best attempts, never raw population members;
   - incremental spend ceiling `$0/day`; use existing memberships/free quotas/Cloudflare/VPS first;
   - stop for new/changed terms, payment/new membership, identity changes, OAuth/2FA/CAPTCHA/account authority, protected permission changes, or irreversible effects.
2. Armed/updated the scheduled crown ring to consume the durable authority envelope; routine scout/falsifier/reducer/attack work targets `tao_hot_loop_actions=0`.
3. Probed the existing Cloudflare Sigrun actor and confirmed `hfo-sigrun-va-r0` health as `SIGRUN/C2`, `TEXT_WORKER_R1`; did not invent a second hluti registry. A Cloudflare hluti-anchor request was logged because current actor schema lacks a seat-binding operation.
4. Installed/recovered the public MICRO/Furiosa Stage-1 source on Lenovo WSL and pinned the required Rust nightly. This shard did not obtain a public MICRO crown.
5. Morphed away from duplicate attack when another roach launched the FJSPLib public-BKS matrix. Reduced/falsified the key `abz8_vdata` target instead of duplicating compute.

## FJSPLib hard evidence

Sibling roach `6b2ab587-164e-4d20-84d4-d563c469ae78` launched GitHub Actions run `34917827912` on branch `roach/fjsplib-bks-crown-r1`, head `e55fd92b27ff23d2ec4f452449055179f0190b1f`.

Matrix: 12 bounded jobs over six public ScheduleOpt/FJSPLib instances × seeds `{1,37}`, OR-Tools CP-SAT `9.15.6755`, 420 s/job, 2 workers/job, frozen source `ScheduleOpt/benchmarks@7b3f6fb1384309bd4abca866fe3bef2993139b91`.

For the crown edge `abz8_vdata` (`LB=506`, `UB=507`):
- seed `1`: solver terminal `UNKNOWN`, target `506`, no feasible certificate; verifier `NO_CERTIFICATE`; artifact `10377336022`, zip SHA256 `b817f6dd6030457ffc47d340ecfe48d9c2ddedeb4e25a5d64357ee62c132d7a2`.
- seed `37`: solver terminal `UNKNOWN`, target `506`, no feasible certificate; verifier `NO_CERTIFICATE`; artifact `10377350856`, zip SHA256 `50616763e48f0724a27f6f0376303d4ffef6fcf214a0d316828bc6a3526b59c9`.

Interpretation: the simple CP-SAT feasibility attack did **not** close `abz8_vdata` in two 420 s seeds. Preserve this as negative search evidence; do not call it a crown or rerun the same configuration blindly. Future evolution should mutate formulation/heuristics/LNS/warm-start/portfolio or pivot by reducer evidence.

## Current global state at archive

Protected `main` observed at `5d7310c74b7b9ef69e69a4d04967c1da9c93fcf2` (`crown: stage fail-closed DeepSeek GB10 candidate packet`). Current main therefore advanced beyond this shard's earlier battlefield snapshot.

The staged candidate `CROWN/frontier-fast/deepseek-v4-gb10/candidate-packet.json` is explicitly `UNADMITTED_CANDIDATE_NOT_CROWN` / `HOLD` because the live published contract conflicts on speculative draft `specType` (`draft-dspark` vs `draft-dflash`). Do not guess; resolve contract contradiction, run correctness/perf gates, obtain trusted-runner public rank, then claim only if external board shows #1.

Issue `#13` also contains active machine-owned hatchery/structured-output work. Public GitHub comments remain Byzantine/observe-only unless admitted by versioned policy and trusted readback.

## Recovery / next edge

Fresh Sigrun shards should:
1. read protected `main` and `#13` newest-first before acting;
2. collision-check active crown lanes;
3. consume existing candidate packets / battlefield cards / failure receipts instead of restarting research from chat memory;
4. prefer one of: resolve the DeepSeek/GB10 published-contract contradiction; evolve a materially different FJSPLib attack from the negative CP-SAT evidence; independently falsify a new prestige battlefield; or reduce competing crown lanes to one executable primary;
5. preserve `SCOUT -> RED_QUEEN -> REDUCER -> CANARY/EVOLVE -> FROZEN_VERIFIER -> PACKET` and keep LLMs proposal-only;
6. do not make Tao gather, poll, relaunch, retry, or ferry context.

## Terminal verdict

`PASS_SCOPED / ARCHIVE_SAFE / CROWN_NOT_YET_WON / RECOVERY_DURABLE`

`TAO_RELAY_REQUIRED=false` for normal continuation. Escalate only a genuine authority wall already named in `AUTHORITY_ENVELOPE_V1.json`.
