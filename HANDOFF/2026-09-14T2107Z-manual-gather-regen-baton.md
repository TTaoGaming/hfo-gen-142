# BATON — Manual Gather / Thread Archive / Virtual-Actor Regen

UTC: `2026-09-14T21:07Z`

Tao is manually gathering results across multiple ChatGPT threads while doing unrelated work. This is operator burden and a control-plane failure signal, not acceptable steady-state operation.

## STRIFE
`MANUAL_GATHER_REQUIRED -> swarm state is not yet self-reducing into one durable actor/world-state surface.`

The current thread will be archived. The regenerated virtual actor must recover from GitHub durable state and must not ask Tao to restate this thread.

## Durable state reached
- `HOLON_MISSION_COMMAND_CONTRACT.md` formalizes persistent actor/HQ identity, disposable carriers, temporary pyramid delegation, machine Herald/Raven receipts, promoted skills, heritage, reputation, and domain-agnostic missions.
- Executable enforcement exists: `schemas/holon-mission-v1.schema.json`, `tools/holon_gate.py`, `ops/vps_exec_guard.py`, `tests/test_holon_gate.py`, `ops/install_vps_forcing_functions.sh`, plus AGENTS enforcement.
- Fresh Lenovo runtime assay: holon + crown + gateway forcing suites = `11/11 PASS`.
- Existing protected `TTaoGaming/cdev-control#5` was extended only as the admitted Actions/Oracle wake path. Head `32cd5e1854b8d96b59de091fb008523f4a52f0f7` triggered run `34889286895` on `hfo-cdev-oracle-r1`.
- Oracle forcing-function installation/assay step PASS. Artifact `10365999409`; digest `sha256:dfa33edc2090c9e75ece93d2eceee16bbd08142c3c93dcbb5948c4790ca2791f`.
- Existing A/B Sigrun step failed closed at exact remaining auth seam: `SVA_TOKEN_MISSING`.
- `cdev-control#5` remains open/mergeable/protected; do not weaken protection for convenience.
- OVH Desktop Commander ping is live, but a reliable terminal/file execution witness was not established in this thread.

## Architecture lock
`GitHub intent/evidence -> Sigrun Cloudflare DO claim/fence/deadline/terminal -> Actions/VPS disposable carrier -> frozen verifier -> Result -> ConsumerAck -> automatic next mission`

- `actor_id != carrier_id`.
- Provider/model is leaf donor, never control plane.
- No second scheduler/queue/lease/memory authority.
- AI is leverage, not the default business domain.
- Tao relay defaults false except real secret/OAuth/2FA/payment/permission/protected-merge/irreversible-submit boundaries.

## Exact next edges
### P0 — autonomy
Provision the existing `SVA_TOKEN` into the approved `cdev-control` Actions secret/environment scope through authorized secret management. Re-run the existing A/B assay unchanged.

PASS_FULL requires:
`A post-claim fault -> durable recovery/terminal + HRIST/P7 ack -> machine-triggered B -> B terminal+ack -> exact Actions run/job/artifact provenance`, laptop not required, Tao hot-loop/context ferry = 0.

### P1 — durable holon learning
After P0, assay one real actor mission where the same `actor_id` survives carrier replacement and a verified/promoted skill from mission A is consumed in mission B. Prose or chat memory is not inheritance; promotion requires verifier + ConsumerAck.

### P2 — crown/income
Keep `CROWN_FITNESS_CONTRACT.md` fail-closed. Search is lawful-domain agnostic. Prestige floor before win probability. `FRONTIER_REQUIRED` jobs fail closed without an admitted frontier route. Public incumbent/champion artifacts may be donor genes only when target rules permit; mutate via successive halving against frozen official evaluators.

## Recovery pointer
`Recover TTaoGaming/hfo-gen-142#13 newest-first. Find BATON — MANUAL GATHER / THREAD ARCHIVE / VIRTUAL-ACTOR REGEN at 2026-09-14T21:07Z and HANDOFF/2026-09-14T2107Z-manual-gather-regen-baton.md. Verify mutable state fresh. Do not ask Tao to ferry context. Close P0 SVA_TOKEN Actions boundary first; then assay durable holon survival/learning; keep crown search domain-agnostic and prestige-gated.`

`TAO_RELAY_REQUIRED=true` only for the authorized secret/approval boundary above; otherwise `false`.
