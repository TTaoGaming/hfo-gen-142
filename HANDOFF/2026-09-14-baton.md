# GEN142 baton handoff — 2026-09-14

## Recovery pointer
Fresh carrier: read this file, then `HIVE_R0.md`, issue #2 newest-first, issue #3 newest-first, and branch `contract/portable-hatchery-larva-r0` at `e598157c2dcc4604a867d70d1cb3e069732b9ff0`.

## Current truth
- Gen142 is the canonical active generation; older generations are donor/evidence archives.
- G1 Cloudflare caller-replacement/idempotency assay: scoped PASS. Same durable work survived caller replacement and redeploy; duplicate replay did not create a second accepted effect; `accepted_effect_count=1`; durable ConsumerAck exists. Scope remains synthetic/no-effect.
- G2 Lenovo-free default-path VPS execution: HOLD / NOT PROVEN. Oracle and OVH are directly reachable, but the current harness requires an explicit device target when multiple devices are online. Reachability is not an admitted/default executor. Oracle's explicit evolution service was observed disabled/inactive; OVH has useful COTS pieces but no proven Gen142 default executor seam.
- Portable Hatchery work is preserved on remote branch `contract/portable-hatchery-larva-r0` at `e598157...`; local checkout was clean and remote branch matched that SHA at handoff.
- The portable bootstrap adapter was exercised on laptop + Oracle ARM64 + OVH x86_64 with the same canonical fixture output. This is bootstrap evidence, not host admission.
- Two sequential finite packing generations ran on Oracle through the native supervisor with a frozen exact evaluator and zero provider calls. They retained the same champion; this is useful finite Evo evidence, not full G3 unattended autonomy.
- Full G3 remains HOLD until bounded work is connected through the admitted/default VPS lifecycle and survives interruption/recovery with stale-fence rejection, bounded disk, independent evaluation, zero duplicate accepted effects, no Tao routing, and no Lenovo dependency.

## Chat cloud-input bridge
- Account-B Playwright bridge recovered and proved live on laptop.
- Fresh canary `LAUNCH_NONCE FB076AB722C3` submitted once at `Latest + XHIGH / Extra High`; child independently START-ACKed the same nonce in #2 and self-attested GPT-5.6 Sol / ChatGPT.
- True headless receives provider challenge; do not bypass. Proven mode is headful off-screen.
- Bridge guards strengthened: stale-composer HOLD, exact prompt readback before Enter, durable nonce non-replay, possible-send ambiguity => HOLD/no resend. Local bridge regression suite reached 13/13 PASS.
- VPS browser clone is not admitted. Clone code/runtime only; never raw cookies/profile. It needs a fresh provider login, no-send settings assay, and unique-nonce ACK before `VPS_LIVE`.
- A later attempt to fan out a 2-Ling + 2-Roach Account-B wave from this carrier was platform safety-blocked before sends; do not claim those four workers exist.

## Hive / knowledge direction
- Desired architecture: fresh carrier = uncommitted Larva capacity -> Gateway recovery -> demand-first capability/admission -> JIT morph -> Agent Skill -> admitted tools/effect ceiling -> bounded PDSA -> evidence -> ConsumerAck -> release/remorph.
- A2A Agent Card = service discovery/served truth; Agent Skills = procedures; MCP/native APIs = tools; GitHub/Cloudflare evidence = admission; none imply effect authority.
- Twinlings explore/falsify. Roaches preserve/fan-in only after durability is proven. Reducers must turn issue-terminal streams into compact canonical Strife/Splendor + genes/world state so Tao stops repeating context.

## Next bounded edges
1. Reduce newest #2 falsifiers and do not overclaim G2.
2. Choose one existing COTS VPS executor as the admitted/default owner; prove one deterministic no-effect work_id through Gateway -> admission -> VPS -> durable readback with Lenovo not required.
3. Then run/restart two generations through the same admitted path and require frozen evaluator+runtime fingerprint, bounded disk, zero duplicate accepted effects, durable lineage, independent falsification, and no Tao relay.
4. Continue moving chat-input bridge toward VPS only after normal one-time login; no credential/session copying.

## Operator protection
No Tao context ferry, routing, routine approvals, cleanup, or babysitting. `UNKNOWN` never becomes TRUE. No new control plane when COTS owns the function. Recommendation before falsification is untrusted.
